<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubUserApp" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.GithubUserApp" parent="Base.Theme.GithubUserApp" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_github_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.GithubUserApp</item>
    </style>

</resources>